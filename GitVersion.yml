mode: Mainline
branches:
  feature:
    regex: ^feature?[/-]
    mode: ContinuousDeployment
    source-branches: ["main", "master"]
    tag: feature-{BranchName}
ignore:
  sha: []
merge-message-formats: {}
